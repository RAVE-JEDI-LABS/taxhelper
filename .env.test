# Test environment configuration
NODE_ENV=test
PORT=3099

# Mock Firebase (tests use mocked services)
FIREBASE_PROJECT_ID=test-project
FIREBASE_STORAGE_BUCKET=test-bucket.appspot.com

# Disable external integrations in tests
SKIP_TWILIO_VALIDATION=true
ALLOW_DEMO_AUTH=false

# API URL for integration tests
NEXT_PUBLIC_API_URL=http://localhost:3099
